using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaterTowerConcept : MonoBehaviour
{
    public GameObject Concept;

    // Start is called before the first frame update
    void Start()
    {
        Concept.SetActive(true);
    }
}
